Como o Tkinter não é instalado via `pip`, o seu arquivo deve servir como um guia de instalação de sistema, o que é uma prática avançada e correta.

```text
# Bibliotecas Nativas (Não requerem pip install):
# - tkinter
# - random
# - messagebox

# Dependência de Sistema para LINUX (Ubuntu/Debian):
# Execute: sudo apt install python3-tk
